# Copy to .env.local (git-ignored) and fill in.
# Optional: enables accurate ID card reading with Claude (Task 2).
# Without it, the National ID page falls back to on-device Tesseract OCR.
ANTHROPIC_API_KEY=
